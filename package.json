{
  "name": "markdown-reader",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "esbuild frontend/main.js --bundle --outfile=src/bundle.js --format=esm --minify && cp frontend/styles.css src/styles.css",
    "dev": "esbuild frontend/main.js --bundle --outfile=src/bundle.js --format=esm --sourcemap && cp frontend/styles.css src/styles.css",
    "tauri": "tauri",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "@tauri-apps/plugin-clipboard-manager": "^2.3.2",
    "highlight.js": "^11.11.1",
    "katex": "^0.16.42",
    "marked": "^17.0.5",
    "mermaid": "^11.13.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1",
    "esbuild": "^0.27.4"
  }
}
