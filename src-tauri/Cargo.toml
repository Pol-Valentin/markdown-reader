[package]
name = "markdown-reader"
version = "0.1.0"
description = "Lightweight Markdown reader with Mermaid support"
authors = ["pol"]
license = "MIT"
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
log = "0.4"
tauri = { version = "2", features = ["image-png"] }
notify = "7"
notify-debouncer-mini = "0.5"
chrono = { version = "0.4", features = ["serde"] }
dirs = "6"
zbus = { version = "5", default-features = false, features = ["tokio", "blocking-api"] }
tokio = { version = "1", features = ["net", "io-util", "sync", "rt"] }
libc = "0.2"
tauri-plugin-clipboard-manager = "2.3.2"
base64 = "0.22"

[dev-dependencies]
tempfile = "3"

[patch.crates-io]
wry = { path = "../vendor/wry" }

[profile.release]
# Workaround for macOS 26 Tahoe: WKWebView fails to render in release builds
# unless a debug_assertions-gated code path is active in objc2 / wry.
# Root cause not fully pinpointed; enabling debug-assertions in release is the
# smallest effective workaround until upstream investigates.
debug-assertions = true

